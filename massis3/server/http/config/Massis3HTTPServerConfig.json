{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "http://schemas.massisframework.com/massis3/server/http/config/Massis3HTTPServerConfig.json",
    "type": "object",
    "properties": {
        "host": {
            "description": "the host name",
            "type": "string",
            "default": "localhost"
        },
        "port": {
            "description": "the port where the app should be running",
            "type": "integer",
            "default": 8080
        }
    }
}
